// you are give 4 graphs but these all are components of single graphs

// you are give 4 graphs but these all are components of single graphs

int v = 14;
visted array =>  int visted[V] = {0};
for(int i=0; i<V; i++){
    if(!visted[i]){ // if node is not visited 
        // apply traversal algorithm (bfs and dfs )
    }
}
